name: partfinder_mobile
description: "App móvil para consultar patentes chilenas conectada a PartFinder 360"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  url_launcher: ^6.2.5
  webview_flutter: ^4.7.0
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
